
  //int mag() {
  //  return (int)sqrt(x * x + y * y);
  //}

  //Vector setLength(int len) {
  //  //println("norm",div(mag()/10));
  //  return div(mag()/len);
  //}

  //Vector limit(float max) {
  //  float len = mag();
  //  if (len > max) {
  //    float factor = max / len;
  //    return mult(factor);
  //  }
  //  return this;
  //}
